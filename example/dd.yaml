# Copyright 2020 BULL SAS All rights reserved
components:
  dd:
    command: dd if=/dev/zero of=file.test
    parameters:
      bs:
        cmd_var: True
        type: int
        default: 1000
        suffix: "M"
      count:
        cmd_var: True
        type: int
        default: 5
      test:
        env_var: True
        type: str
        default: "toto"
